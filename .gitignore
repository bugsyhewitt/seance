# Build output
/seance
/bin/
/dist/
*.exe

# Validation run artifacts (logs, PIDs, state — never commit)
/validation-v0.1/
/validation-*/

# PAI session work memory (local only)
/MEMORY/WORK/

# State and cache
.seance/
*.state.json

# Secrets — never commit these
.env
.env.*
*.pem
*.key
*.pfx
*.p12
id_rsa
id_ed25519
credentials.json
secrets.yaml
secrets.yml

# Go toolchain
vendor/

# IDE
.idea/
.vscode/
*.swp
